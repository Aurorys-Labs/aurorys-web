version: "3.8"

services:
  website:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: dev
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    restart: unless-stopped
